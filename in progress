def fibFact(num):
    count = num -1
    while num > 1:
        if num%count != 0:
            count -= 1
        if num%count == 0:
            num = count
            return fibFact(fibFact(num))
            
fibFact(36)
    


        
    
        

            
